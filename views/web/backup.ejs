<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Backups - <%= appName %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Inter", sans-serif;
      }
      .glass-card {
        background: rgba(31, 41, 55, 0.7);
        backdrop-filter: blur(12px);
        border: 1px solid rgba(255, 255, 255, 0.1);
        transition: all 0.3s ease;
      }
      .gradient-bg {
        background: linear-gradient(135deg, #1e293b 0%, #0f172a 100%);
      }
      .gradient-text {
        background: linear-gradient(135deg, #60a5fa, #3b82f6);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
      }
      .stat-card {
        background: rgba(30, 41, 59, 0.5);
        border: 1px solid rgba(71, 85, 105, 0.2);
        transition: all 0.3s ease;
      }
      .stat-card:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px -5px rgba(59, 130, 246, 0.2);
      }
      .text-muted {
        color: rgba(255, 255, 255, 0.6);
      }
      .backup-item:hover {
        background: rgba(71, 85, 105, 0.3);
      }
    </style>
  </head>

  <!-- Sidebar -->
  <%- include('../components/sidebar') %>

  <body class="gradient-bg text-white min-h-screen flex flex-col sm:ml-64">
    <div class="flex-grow p-8">
      <div class="max-w-6xl mx-auto">
        <div class="flex items-center gap-4 mb-8">
          <h1 class="text-3xl font-bold gradient-text">Backup Manager</h1>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
          <div class="stat-card rounded-xl p-6">
            <div class="flex items-center gap-3 mb-4">
              <div class="bg-blue-500/10 p-2 rounded-lg">
                <i data-lucide="hard-drive" class="w-5 h-5 text-blue-400"></i>
              </div>
              <h3 class="text-lg font-medium">Total Backups</h3>
            </div>
            <p class="text-3xl font-bold mb-2" id="totalBackups">
              <%= backups.length %> Backup<%= backups.length > 1 ? "s" : "" %>
            </p>
            <p class="text-muted text-sm">
              Total size: <%= totalSizeFormatted %>
            </p>
          </div>

          <div class="stat-card rounded-xl p-6">
            <div class="flex items-center gap-3 mb-4">
              <div class="bg-green-500/10 p-2 rounded-lg">
                <i data-lucide="clock" class="w-5 h-5 text-green-400"></i>
              </div>
              <h3 class="text-lg font-medium">Last Backup</h3>
            </div>
            <p class="text-3xl font-bold mb-2" id="lastBackup">
              <% if (lastBackupTime) { %> <%= lastBackupTime %> <% } else { %> 0
              <% } %>
            </p>
            <p class="text-muted text-sm">Time elapsed since the last backup</p>
          </div>

          <div class="stat-card rounded-xl p-6">
            <div class="flex items-center gap-3 mb-4">
              <div class="bg-purple-500/10 p-2 rounded-lg">
                <i
                  data-lucide="shield-question-mark"
                  class="w-5 h-5 text-purple-400"
                ></i>
              </div>
              <h3 class="text-lg font-medium">SOON</h3>
            </div>
            <p class="text-3xl font-bold mb-2" id="nextBackup">SOON</p>
            <p class="text-muted text-sm">SOON</p>
          </div>
        </div>

        <form action="/web/backup/create" method="POST">
          <div class="glass-card rounded-xl p-6 mb-6">
            <div class="flex justify-between items-center mb-6">
              <h2 class="text-xl font-semibold">Create New Backup</h2>
            </div>
            <div class="space-y-4">
              <div class="flex items-center gap-4">
                <div class="flex-1">
                  <label class="block text-sm font-medium mb-1"
                    >Backup Name</label
                  >
                  <input
                    type="text"
                    name="backupName"
                    class="w-full bg-gray-800 border border-gray-700 rounded-lg px-4 py-2"
                    placeholder="my-website.fr"
                    required
                  />
                </div>
                <div>
                  <label class="block text-sm font-medium mb-1">Website</label>
                  <select
                    name="websiteId"
                    class="bg-gray-800 border border-gray-700 rounded-lg px-4 py-2"
                    required
                  >
                    <% if (websites.length > 0) { %> <% websites.forEach(site =>
                    { %>
                    <option value="<%= site.uuid %>"><%= site.name %></option>
                    <% }) %> <% } else { %>
                    <option disabled>No website found</option>
                    <% } %>
                  </select>
                </div>
              </div>
              <div class="flex justify-end gap-3">
                <button
                  type="submit"
                  class="px-4 py-2 bg-blue-600 rounded-lg hover:bg-blue-700 transition flex items-center gap-2"
                >
                  <i data-lucide="plus" class="w-4 h-4"></i>
                  Create Backup
                </button>
              </div>
            </div>
          </div>
        </form>

        <div class="glass-card rounded-xl p-6">
          <div class="flex justify-between items-center mb-6">
            <h2 class="text-xl font-semibold">Backups</h2>
            <div class="flex items-center gap-3">
              <!--<button class="p-2 rounded-lg hover:bg-gray-800/50 transition">
                <i data-lucide="refresh-cw" class="w-5 h-5"></i>
              </button>-->
              <select
                class="bg-gray-800 border border-gray-700 rounded-lg px-3 py-1 text-sm"
              >
                <option>All Backups</option>
              </select>
            </div>
          </div>

          <div class="space-y-2">
            <% if (backups.length > 0) { %> <% backups.forEach(function(backup)
            { %>
            <div
              class="backup-item flex items-center justify-between p-4 rounded-lg transition"
            >
              <div class="flex items-center gap-4">
                <div class="bg-blue-500/10 p-3 rounded-lg">
                  <i data-lucide="hard-drive" class="w-6 h-6 text-blue-400"></i>
                </div>
                <div>
                  <h3 class="font-medium">
                    <%= backup.name %> - <%= backup.created_at.split(" ")[0] %>
                  </h3>

                  <p class="text-sm text-muted">
                    Created <%= backup.created_at %> • <%=
                    formatBytes(backup.size) %>
                  </p>
                </div>
              </div>
              <div class="flex items-center gap-3">
                <button
                  class="p-2 rounded-lg hover:bg-gray-800/50 transition"
                  title="Download"
                  onclick="window.location.href='/web/backup/download/<%= backup.id %>'"
                >
                  <i data-lucide="download" class="w-5 h-5"></i>
                </button>
                <form
                  action="/web/backup/delete/<%= backup.id %>"
                  method="POST"
                >
                  <button
                    type="submit"
                    class="p-2 rounded-lg hover:bg-red-500/20 transition"
                    title="Delete"
                  >
                    <i data-lucide="trash-2" class="w-5 h-5 text-red-400"></i>
                  </button>
                </form>
              </div>
            </div>
            <% }) %> <% } else { %>
            <div class="p-4 rounded-lg bg-gray-800/30 text-center text-muted">
              It appears that no backups are currently stored.
            </div>
            <% } %>
          </div>

          <div
            class="mt-6 flex justify-between items-center text-sm text-muted"
          >
            <div>Showing 0 of <%= backups.length %> backups</div>
            <div class="flex items-center gap-2">
              <button
                class="p-2 rounded-lg hover:bg-gray-800/50 transition disabled:opacity-50"
                disabled
              >
                <i data-lucide="chevron-left" class="w-4 h-4"></i>
              </button>
              <span>Page 1 of 1</span>
              <button class="p-2 rounded-lg hover:bg-gray-800/50 transition">
                <i data-lucide="chevron-right" class="w-4 h-4"></i>
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>
    <%- include('../components/footer') %>
    <script src="/js/sidebar.js"></script>

    <script>
      lucide.createIcons();

      document.addEventListener("DOMContentLoaded", () => {
        console.log("Backup manager loaded");
      });
    </script>
  </body>
</html>
