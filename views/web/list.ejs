<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Websites & Domains - <%= appName %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Inter", sans-serif;
      }
    </style>
  </head>

  <!-- Sidebar -->
  <%- include('../components/sidebar') %>

  <body
    class="bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900 text-white min-h-screen flex flex-col sm:ml-64"
  >
    <!-- Main content -->
    <div class="flex-grow p-6">
      <h1 class="text-2xl font-bold mb-6">My Websites</h1>

      <% if (websites.length === 0) { %>
      <p class="text-gray-300">You don’t have any website yet.</p>
      <% } else { %>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <% websites.forEach(site => { %>
        <div
          class="bg-gray-800 rounded-xl shadow p-4 hover:bg-gray-700 transition"
        >
          <h2 class="text-xl font-semibold mb-2"><%= site.name %></h2>
          <p class="text-sm text-gray-400 mb-1">
            Port: <span class="text-white font-medium"><%= site.port %></span>
          </p>
          <p class="text-sm text-gray-400">
            Disk limit:
            <span class="text-white font-medium"
              ><%= site.disk_limit %> MB</span
            >
          </p>
        </div>
        <% }) %>
      </div>
      <% } %>
    </div>

    <!-- Footer -->
    <%- include('../components/footer') %>
    <script src="/js/sidebar.js"></script>
  </body>
</html>
