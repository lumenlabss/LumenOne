<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Customers - <%= appName %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Inter", sans-serif;
      }
    </style>
  </head>
  <!--Navigations-->
  <%- include('../../components/sidebar') %>

  <body
    class="bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900 text-white min-h-screen flex flex-col sm:ml-64"
  >
    <!-- Main content -->
    <div class="flex-grow">
      <h1 class="text-2xl font-bold mb-4">Edit User</h1>
      <form action="/web/admin/customers/edit/<%= user.id %>" method="POST">
        <div class="mb-4">
          <label for="username" class="block text-sm font-medium mb-2"
            >Username</label
          >
          <input
            type="text"
            id="username"
            name="username"
            value="<%= user.username %>"
            class="border border-gray-300 rounded px-3 py-2 w-full"
          />
        </div>
        <div class="mb-4">
          <label for="rank" class="block text-sm font-medium mb-2">Rank</label>
          <input
            type="text"
            id="rank"
            name="rank"
            value="<%= user.rank %>"
            class="border border-gray-300 rounded px-3 py-2 w-full"
          />
        </div>
        <button
          type="submit"
          class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"
        >
          Save Changes
        </button>
      </form>
      <button
        class="bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-4 rounded mt-4"
        onclick="window.location.href='/web/admin/customers'"
      >
        Cancel
      </button>
    </div>

    <!--Footer-->
    <%- include('../../components/footer') %>
    <script src="/js/sidebar.js"></script>
  </body>
</html>
