<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Modifier un utilisateur - <%= appName %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Inter", sans-serif;
      }
    </style>
  </head>

  <!-- Sidebar -->
  <%- include('../../components/sidebar') %>

  <body
    class="bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900 text-gray-100 min-h-screen flex flex-col sm:ml-64"
  >
    <!-- Main content -->
    <div class="flex-grow p-6">
      <h1 class="text-2xl font-bold mb-6">Modifier l'utilisateur</h1>

      <form
        action="/web/admin/customers/edit/<%= userToEdit.id %>"
        method="POST"
      >
        <div class="mb-4">
          <label for="username" class="block text-sm font-medium mb-2"
            >Nom d'utilisateur</label
          >
          <input
            type="text"
            id="username"
            name="username"
            value="<%= userToEdit.username %>"
            class="border border-gray-300 text-black rounded px-3 py-2 w-full"
            required
          />
        </div>

        <div class="mb-4">
          <label for="rank" class="block text-sm font-medium mb-2">Rang</label>
          <input
            type="text"
            id="rank"
            name="rank"
            value="<%= userToEdit.rank %>"
            class="border border-gray-300 text-black rounded px-3 py-2 w-full"
            required
          />
        </div>

        <button
          type="submit"
          class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"
        >
          Sauvegarder les modifications
        </button>

        <button
          type="button"
          class="bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-4 rounded ml-2"
          onclick="window.location.href='/web/admin/customers'"
        >
          Annuler
        </button>
      </form>
    </div>

    <!-- Footer -->
    <%- include('../../components/footer') %>
    <script src="/js/sidebar.js"></script>
  </body>
</html>
