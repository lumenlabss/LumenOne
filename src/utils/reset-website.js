// console.log("/utils/reset-website.js loaded"); // To confirm that the page has been loaded correctly
const fs = require("fs");
const path = require("path");
const db = require("../db");

async function resetWebsite() {}
